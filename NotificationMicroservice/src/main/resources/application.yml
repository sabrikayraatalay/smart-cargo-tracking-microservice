server:
  port: 9092

spring:
  application:
    name: notification-service

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  #GMAIL
  mail:
    host: smtp.gmail.com
    port: 587
    username: "sabrikayraa@gmail.com" #
    password: ${APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/